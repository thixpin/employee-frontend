query($id: ID!) {
    employee(id: $id) {
        id
        name
        salary
        position
        phone
        email
        address
        photo
        joined_at
        is_active
        department {
            id
            name
        }
        created_at
        updated_at
    }
}